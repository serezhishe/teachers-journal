import { SeparateWordsPipe } from './separate-words.pipe';

describe('SeparateWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new SeparateWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
